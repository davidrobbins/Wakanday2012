﻿﻿/* Javascript Fundementals *//* 6. Arrays *///var foo = new Array("red", "blue", "green"),//	len = foo.length;//foo[1];//len/* Literal constructor is much more common. *//* We can get and set element values. */var bar = [11, "wakanda", true];//var value = bar[0];//value;//bar[0] = 12;//value = bar[0];//value;/* Add new elements. *///bar[3] = "Red";//newValue = bar[3];//newValue;/* Array "push" method is better way to add elements. *///bar.push("one");//bar.push("tow");//bar.length;/* Array "concat" method. *///var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2);//people;/*Array "join" method. *///var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2);//var joined = people.join(", ");//joined;/*Array "sort" method. *///var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2);//var sorted = people.sort();//sorted;