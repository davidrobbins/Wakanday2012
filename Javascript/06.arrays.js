﻿﻿/* Javascript Fundementals *//* 6. Arrays *//* Create our array object with the "new" keyword. *//*var foo = new Array("red", "blue", "green"),	len = foo.length;	foo[1];len*//* Literal constructor is much more common. *///var bar = [11, "wakanda", true];//var first = bar[0], //We access each individual array element.//	last = bar[2];//first + " " + last;//bar[0] = 12; // We can set values of array elements.//bar;//bar[3] = "Red"; //We can add new elements.//bar//bar.push("Dallas"); //Array "push" method is another way to add elements.//bar.push("Houston");//bar;/* Array objects have methods. *///var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2); //Array "concat" method.//people;//var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2);//var joined = people.join(", "); //Array "join" method.//joined;//var names = ["Bob", "Carol"],//	names2 = ["Ted", "Alice"];//var people = names.concat(names2);//var sorted = people.sort(); ////Array "sort" method.//sorted;